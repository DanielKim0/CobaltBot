import os
import random
import discord
from dotenv import load_dotenv
from discord.ext import commands

class EU4Cog(commands.Cog):
    def __init__(self, bot):
        self.bot = bot
    
    async def cog_check(self, ctx)

    @commands.command(name=idea, aliases=[])
    async def 